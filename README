Build essentials:
sudo apt-get install g++ unar cmake libssl-dev zlib1g-dev

Install libraries:
./install.sh

How to calculate differences:
Usage: ./GoertzelLocalization -X <path to real data> <path to simulated data>
Example: make clean && make -j 9 && ./bin/GoertzelLocalization -X Distances\ level\ 3 Test\ level\ 3

How to build all:
./create.sh

How to run distance calculation:
./create.sh run

FIRST USE:
1. Install build essentials using the command above
(1.1. Install libraries using the command above (probably is not needed if the computer is running x64 Linux which it should))
2. Compile using ./create.sh
3. Run using ./create.sh run, after adjusting IPs